machine:
  python:
    version: 3.7.0

dependencies:
  pre:
    - sudo apt-get update
    - pip install -U pip
    - pip install -r packages_requirements.txt

test:
  post:
    - cd [manage.py-directory]; python setup.py test